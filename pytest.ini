[pytest]
testpaths = tests
addopts = -ra
markers =
    gui: requires QGIS / Qt (run offscreen; skipped if qgis is not importable)
    integration: hits the live FastGIS API (opt-in; set GROUNDTRUTHER_API_KEY)
